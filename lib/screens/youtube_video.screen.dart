import 'package:flutter/material.dart';

class FreeClassVideo extends StatelessWidget {
  const FreeClassVideo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text('Welcome to Free class')],
      ),
    );
  }
}

import 'package:flutter/material.dart';
import 'package:test1_app/containts.dart';

class McqAppLogin extends StatelessWidget {
  const McqAppLogin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
      child: Text(
        "Login Now",
        style: Kcardtitle,
      ),
    ));
  }
}
